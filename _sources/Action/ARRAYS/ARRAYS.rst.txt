=========
配列(ARRAYS)
=========

------

変数をインデックスで設定(SetVariableAtIndex)
----------------------
.. image:: ../../img/action/arrays/arrays.png

指定された **配列** の **変数** を検索または初期化し、指定された **配列** の指定されたインデックス(Index)に提供された値を保存します。

パラメーター
  + 変数(Variable)
  + 数値（Index）
  + あらゆるタイプ（Value）
